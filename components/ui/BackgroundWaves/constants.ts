export enum ParticleColor {
  ThemeLight = 0x0c0a09,
  ThemeDark = 0xfafaf9,
}

export enum ParticlePhysics {
  Seperation = 40,
  AmountX = 130,
  AmountY = 35,
  Radius = 0.1,
  // Contorls the speed of the wave animation effect.
  Speed = 0.1,
}

export enum CameraPosition {
  X = 0.35,
  // Changes how far back you can see towards horizon.
  Y = 150,
  // This is how close or far the particles are seen.
  Z = 300,
}
